# Database credentials — copy to .env and fill in real values
DB_DATABASE=laravel
DB_USERNAME=laravel
DB_PASSWORD=
DB_ROOT_PASSWORD=

# Set to true to run migrations on container startup
RUN_MIGRATIONS=true
